# 치킨 조리 게임 — 기존 픽셀 스타일 / TV3 전용

`chicken_game.html` 하나가 담기 → 썰기 → 굽기 → 결과를 모두 진행합니다. TV3 전용입니다. 기존 px/ 캐릭터·주방 배경·픽셀 그릇·Galmuri 폰트·제목을 유지하고, 굽네 오븐사우나에 맞춰 오렌지·빨강 포인트를 안내판과 진행 막대에 적용했습니다. 원래 360×640 픽셀 장면을 보간 없이 확대합니다. 기존 세 단계의 판정·득점·목표·제한 시간은 유지했습니다.

## 관람객 경험

1. 대기 화면에서 손바닥을 펴고 1.6초 유지하면 시작합니다.
2. 각 단계 앞에 손동작 시범이 나오고 자동으로 시작합니다.
3. 담기: 손을 좌우로 움직입니다. 썰기: 손을 빠르게 휘두릅니다. 굽기: 좌우 위치로 온도를 맞춥니다.
4. 손을 놓치면 자동 일시정지합니다. 다시 보여 주면 이어지고, 12초간 돌아오지 않으면 대기 화면으로 복귀합니다.
5. 결과가 9초간 표시되고 자동으로 복귀합니다. 6초 준비 시간 후 손을 한 번 내렸다가 다시 보여 주면 재플레이합니다.

시작·단계 전환·재개·재시작에 관람객의 클릭, 터치, 키보드 입력은 필요하지 않습니다. 카메라 영상은 손 인식에만 사용하고 저장하거나 외부로 전송하지 않습니다. 손 인식 모델·이미지·폰트는 프로젝트 안에서 불러옵니다.

## 전시 시작

운영자가 `start_tv3.command`를 실행하거나 터미널에서 `bash start_game.sh`를 실행합니다. Chrome/Chromium/Edge와 Python 3가 필요합니다. 실행기는 로컬 서버와 별도의 TV3 전시 브라우저 프로필을 사용합니다. 종료는 Command + Q입니다.

첫 설치 시 운영자가 macOS의 카메라 권한, TV 세로 회전, 전체화면이 열릴 디스플레이, 화면 보호기/절전 설정을 준비해야 합니다. 운영체제의 최초 카메라 허용은 웹페이지가 대신할 수 없습니다. 실행기의 브라우저 플래그는 실제 카메라를 사용하면서 브라우저 내부의 권한 대화상자를 생략합니다. 일반 브라우저에서 HTML을 직접 열면 최초 권한 허용이 필요할 수 있습니다.

기본 카메라는 0번입니다. 여러 카메라가 연결되어 있다면 `GAME_CAMERA=1 bash start_game.sh`처럼 지정합니다. 지정한 카메라가 없을 때 다른 카메라를 선택하지 않고 연결 안내를 표시합니다. 운영 중 카메라가 빠지면 재연결을 자동 시도합니다.

## TV3 도면 반영과 현장 보정

기준: DDP H03 + CURVE01, 2026.09.12, 5·8·13쪽.

- TV 본체: 1090 × 1905mm.
- 창: 900 × 1573.405mm, 본체 왼쪽에서 102.801mm, 본체 위에서 311.595mm.
- 창의 꼭짓점 24개를 도면 좌표 그대로 저장했습니다. 미리보기 마스크는 이 다각형을 사용합니다.
- 주요 텍스트는 창 가장자리보다 안쪽에 배치했습니다. 화면 배경은 TV 전체를 채웁니다.
- 카메라 미리보기는 하단 안내문과 겹치지 않도록 작게 배치했습니다.
- 배경을 조금 확대하고 위로 옮겨, 아치 여백과 게임 내부에 같은 이미지·좌표를 사용합니다. 상단에 다른 장면의 조각이 비치거나 안쪽 사각 테두리가 아치와 겹치는 현상을 제거했습니다.
- 단계 안내판은 화면 안에서 위아래로 등장하며, 손 표시와 시작 게이지도 아치 안쪽에 유지합니다. 이는 표시 위치만 보정하며 손 입력·판정 좌표는 바꾸지 않습니다.

**도면에는 베젤을 뺀 발광 화면 크기가 확정되어 있지 않습니다. 현재 배치는 본체 치수를 이용한 임시 기준이며, 실물 TV에 정확히 일치한다고 보증할 수 없습니다.**

`display-config.json`의 `profiles.3.calibration`에 현장 실측값을 입력합니다.

| 설정 | 입력할 값 |
| --- | --- |
| `screenWidthMm`, `screenHeightMm` | 세로 설치 상태에서 실제 빛이 나오는 화면의 가로·세로 mm |
| `bezelLeftMm`, `bezelTopMm` | 본체 왼쪽·위에서 실제 화면이 시작되는 위치 mm |
| `offsetXmm`, `offsetYmm` | 설치 오차 미세 보정. 양수는 오른쪽·아래 |
| `measured` | 위 실측값 적용 후 `true` |

변환식: 화면 x = (창 좌측 + 창 내부 x − 왼쪽 베젤 + 보정 x) × 가로 픽셀 / 실화면 가로. y도 같은 방식입니다. 1080 × 1920과 2160 × 3840 등 실제 브라우저 화면 크기에 맞춰 렌더링됩니다. TV 설정의 오버스캔은 끄고 원본 크기/화면 맞춤을 사용합니다.

`activeHandArea`는 카메라 좌표에서 손을 움직일 범위입니다. 기본 중앙 72% 가로 구간으로 화면 양끝에 도달합니다. 설치 거리에서 실제 손으로 양끝과 썰기 높이를 확인하세요.

## 운영자 점검

로컬 서버 실행 중 다음 주소를 사용합니다.

- 실제 게임: `http://127.0.0.1:8777/chicken_game.html`
- 자동 플레이: `http://127.0.0.1:8777/chicken_game.html?bot=1&preview=1`
- 손 대신 마우스로 동작 확인: `http://127.0.0.1:8777/chicken_game.html?sim=1&preview=1`
- 타공선·안전 영역: `http://127.0.0.1:8777/chicken_game.html?calibrate=1`

`preview=1`은 창 밖을 가려 물리 프레임 뒤에서 보일 모습을 확인합니다. 실제 실행기는 미리보기 마스크 없이 배경을 전체 화면에 채웁니다. 테스트 모드에는 화면 하단에 별도 표시가 나옵니다.

운영자 단축키: G = 도면 가이드, C = 카메라 점검 영상, D = 담기 판정 영역 초록선. `sim=1`일 때 H = 손 숨김/복귀. 현재 프로젝트에는 Matter.js 상자가 없으므로 D는 현재 게임의 실제 담기 판정 범위를 표시합니다.

원본은 `backups/chicken_game.before-exhibition.html`과 `backups/start_game.before-exhibition.sh`에 보관했습니다. 되돌릴 때 HTML 백업을 프로젝트 루트의 `chicken_game.html`로 복사합니다. 백업 폴더 안에서 직접 열면 상대 경로 이미지가 표시되지 않습니다.
