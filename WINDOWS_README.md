# Windows에서 실행하기

같은 TV3 게임입니다. 캐릭터·폰트·로고·손 인식 자료가 모두 폴더 안에 포함되어 있습니다.

## 화면만 먼저 보기

압축을 **모두 풀고** `chicken_game_sim.html`을 Edge 또는 Chrome으로 여세요. 마우스 움직임으로 손동작을 대신합니다. Python과 카메라는 필요 없습니다. 세로 아치와 원래 비율은 일반 창에서도 유지됩니다.

## 실제 카메라 게임

1. Windows 10/11에 Python 3와 Edge 또는 Chrome을 준비하세요. Python 설치 시 **Add Python to PATH**를 선택하세요. 별도의 Python 패키지는 필요 없습니다.
2. Windows 설정에서 카메라의 **데스크톱 앱 액세스**를 허용하세요. TV 디스플레이를 **세로 방향**으로 설정하세요.
3. `start_tv3.bat`을 더블클릭하면 로컬 서버와 TV3 전체화면 게임이 함께 열립니다.
4. 관람객은 손바닥만 보여 주면 시작합니다. 시작·재개·다음 게임에 클릭이나 터치가 필요 없습니다.
5. 종료는 **Alt + F4**입니다. 실행기가 만든 서버도 함께 종료됩니다.

ZIP 내부에서 바로 실행하지 말고 폴더 전체를 먼저 풀어 주세요. HTML만 따로 옮기면 이미지·폰트·손 인식 파일을 찾을 수 없습니다.

실제 게임은 `file://`로 직접 실행하기보다 `start_tv3.bat`을 사용하세요. 브라우저의 로컬 파일 제한으로 손 인식 모델을 불러오지 못하는 문제를 피하기 위해 로컬 서버를 함께 실행합니다. 서버는 이 PC의 `127.0.0.1`에서만 열립니다.

브라우저 또는 카메라 접근을 조직 정책에서 막은 PC는 운영자가 해당 정책을 확인해야 합니다. 실행기는 Windows 카메라 권한이나 조직 정책을 변경하지 않습니다.

## 운영자 설정

- 카메라 번호: 실행 전 `set GAME_CAMERA=1` (기본 0)
- 서버 포트: 실행 전 `set GAME_PORT=8778` (기본 8777)
- 별도 브라우저 경로: `set "CHROME_BIN=C:\Program Files\Google\Chrome\Application\chrome.exe"`
- 일반 창 미리보기: `chicken_game.html` — 세로 아치를 비율 그대로 표시
- 전시 배치: 실행기가 `?display=tv3`로 실행 — 도면 기준 위치와 현장 보정값 적용

TV 실측 보정과 G/C/D 점검 키는 `EXHIBITION.md`를 참고하세요. 첫 설치 후에는 실제 Windows PC와 카메라에서 손 인식 및 전체화면 위치를 확인하세요.
